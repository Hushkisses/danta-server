package kr.danta.core.combat;

/**
 * DEV-063 broad battlefield terrain categories from design v0.3.
 * Exact terrain combat coefficients remain balancing data, not enum behavior.
 */
public enum BattlefieldTerrain {
    PLAIN,
    FOREST,
    MOUNTAIN,
    CANYON,
    COASTAL,
    URBAN_FORTIFIED
}
