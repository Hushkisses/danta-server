# DEV-093 — NPC Nation / StrategicAI state machine

Status: IMPLEMENTED — Windows verification pending.

Execution-plan scope was rechecked: DEV-093 is NPC Nation/StrategicAI state machine (not trade route).

## Implemented
- Existing NationState remains the authoritative nation aggregate; NPC control is explicit metadata layered on an existing nation instead of duplicating Nation.
- NpcNationService register/unregister/list/state lookup.
- StrategicAiPhase lifecycle: IDLE → EVALUATING → DECIDED → EXECUTING → IDLE, with guarded transitions and cancel-to-IDLE.
- Decision key is an opaque identifier only; no unapproved diplomacy/military/economic scoring or balance constants were invented.
- StrategicAI remains core/Paper-independent and separate from future realtime CombatAI, matching design v0.3.
- Korean development verification commands and rejection messages.
- JUnit coverage for registration uniqueness, valid lifecycle, invalid transition.

## Deferred deliberately
- DEV-094 owns NPC alliance/subjugation/annexation behavior.
- Concrete StrategicAI evaluation/scoring/action selection is data-driven future NPC content; DEV-093 only supplies the state-machine boundary.
- NPC-control/phase snapshot persistence is deferred until NPC nations become season-authoritative rather than test registration. Do not infer that development command registration survives restart.
