# DEV-066 — Win-rate / loss CSV report

Status: COMPLETE — Windows automated tests/build and Paper boot verified

## Execution-plan basis
Phase 6 defines DEV-066 as the win-rate/loss CSV report following DEV-065 repeated combat execution. Phase 6 also states that combat randomness should ultimately target roughly ±10%, but the current resolver has no authoritative random component yet; DEV-066 therefore reports current deterministic results rather than fabricating variance.

## Implemented
- CombatSimulationCsv formats DEV-065 aggregate summaries as standards-compatible CSV.
- Stable columns:
  - scenario_id
  - runs
  - first_wins / second_wins / draws
  - first_win_rate / second_win_rate / draw_rate
  - avg_first_losses / avg_second_losses
- Decimal output is locale-independent and fixed to six places.
- Scenario IDs are CSV-escaped when necessary.
- combat-simulator accepts an optional second argument as the CSV output path.
- Parent directories are created automatically and UTF-8 is used.

## Usage
With the representative's existing Gradle executable/path:
`gradle :combat-simulator:run --args="1000 build/reports/combat-balance.csv"`

Direct Gradle invocation is optional for DEV-066 verification because quick-deploy already runs automated tests. Do not install another Gradle just for this command.

## Scope boundary
- No random variance is added by this ticket.
- No new balance coefficients are introduced.
- DEV-063/064 typed terrain/logistics signals remain unconverted until authoritative numeric balancing is defined.
- The CSV is a development balance artifact, not a player-facing feature.

## Automated tests
Tests verify stable headers/rows, locale-independent decimals and CSV escaping.

## Windows verification
1. Stop Paper.
2. Pull main.
3. Run `dev-server\quick-deploy.bat`.
4. Confirm all automated tests pass and deployment completes.
5. Start `dev-server\start-dev.bat` and confirm normal Paper/Danta/PostgreSQL boot.
6. CSV file generation may be checked later when invoking the existing Gradle executable directly is convenient; it is not required to alter PATH or install Gradle.

Keep IMPLEMENTED until Windows automated verification and normal Paper boot succeed.

## Verification result
The representative confirmed on 2026-09-15 that quick-deploy succeeded after the CSV quote-escaping compile fix and the Paper server started and operated normally.
